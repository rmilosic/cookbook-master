export type Cookbook = {
    name: {
        "S": string
    }
    // author: string,
    // language: string,
    // coverPath: string,
    // ocrDate?: number,
    // tocPath?: string,
    // rawTextPath?: string,
    // rawTextPathEn?: string,
    // indexedDate?: number,
    // created: number,
    // updated?: number
}